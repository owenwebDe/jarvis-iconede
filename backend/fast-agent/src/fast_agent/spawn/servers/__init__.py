"""MCP servers shipped with the spawn package.

These are standalone MCP server scripts that can be started by the
orchestrator as subprocess targets::

    uv run python -m fast_agent.spawn.servers.meeting_room_server
    uv run python -m fast_agent.spawn.servers.team_communicate_server
"""
